/// Marker type confirming the package resolves and links; real logic lives in
/// the sibling files added as Phase 0 progresses (TimeAxis, DayBoundary,
/// TagRangeMath, Formatters).
public enum StudyLapseCore {}
