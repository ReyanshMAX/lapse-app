import XCTest

/// Placeholder so the `StudyLapse` scheme has a non-empty test action for CI's
/// "Simulator tests" job. Real simulator-level tests (persistence, recovery,
/// export, fixtures per CLAUDE.md's repo structure) land here in later phases.
final class StudyLapseTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
